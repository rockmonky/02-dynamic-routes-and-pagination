---
interface Props {
    pokemonName: string;
    pokemonImg: string;
}
const { pokemonName, pokemonImg } = Astro.props;

const id = pokemonImg.split("/")[pokemonImg.split("/").length - 2];
const pokemonImageUrl = `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${id}.png`;
console.log(pokemonImageUrl);

// console.log(pokemonImg);
// console.log(pokemonImg.split("/"));
// console.log(pokemonImg.split("/")[pokemonImg.split("/").length - 2]);

// const response = await fetch(pokemonImg);
// const data = await response.json();
// console.log(data["sprites"]["other"]["official-artwork"]["front_default"]);
---

<a
    class="rounded flex flex-col justify-center items-center p-2 shadow-md shadow-purple-600 border-t border-white hover:border-purple-600"
    href={`/pokemons/${pokemonName}`}
>
    <img src={pokemonImageUrl} alt={pokemonName} />
    <span>{pokemonName}</span>
</a>
